import React from 'react';

const Header = () => {
    return (
        <div className='flex flex-row'>
            <h1>Card helper</h1>
            <button>Add new game</button>
        </div>
    );
};

export default Header;